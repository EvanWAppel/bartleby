# Bartleby — Implementation Tasks

Companion to `PRD.md`. Every task is a small, specific unit of work with an ID and a checkbox.

## How to use this file

- **TDD discipline.** Every task that involves code follows red-green-refactor: write the failing test first, watch it fail for the right reason, then implement until it passes. A task is not complete unless its `test:` clause is satisfied and the test is committed.
- **IDs.** Stable two- or three-letter prefix + zero-padded number. New tasks get the next free number in their group; never reuse IDs.
- **Dependencies.** `deps: ID-NNN, ID-NNN` lists hard prerequisites. No deps means the task can start immediately (assuming Phase 0 is done).
- **Parallelism.** Phase 0 is serial — do not start workstreams until V- tasks are all green. After Phase 0, each workstream below is largely independent; one agent per workstream is a reasonable allocation.
- **Definition of done for any task:** failing test written first → tests pass → linter and typechecker pass (`ruff` + `ty` for Python per `agents.md`, project equivalents for Node/web) → committed.

---

## Phase 0 — Vertical Slice (serial, do this first)

The goal of Phase 0 is to prove the hardest architectural claim in the PRD: a ProseMirror+y-prosemirror web client and a textual+y-py TUI client can be live peers on the same Yjs document via a Hocuspocus server. Nothing else is built until this works.

- [x] **V-001** Initialize monorepo: `server/`, `web/`, `tui/`, `ops/`, `docs/`. Root `README.md` with a short "what is this" + how to run each piece. (test: `ls` shows the four dirs; root README mentions all three components.)
- [x] **V-002** Server: bootstrap Node project in `server/` with TypeScript, vitest, prettier, eslint. (test: `npm test` runs and reports 0 tests.)
- [x] **V-003** Server: stand up a bare Hocuspocus instance on a local port, no auth, in-memory storage, no SQLite yet. (test: integration test boots the server and asserts a WebSocket client can connect and complete a Yjs handshake.)
- [x] **V-004** Web: bootstrap web project in `web/` (SvelteKit recommended; document the choice in `web/README.md`). Add vitest + playwright. (test: `npm run dev` serves index page; playwright smoke test loads it.)
- [x] **V-005** Web: single-page editor at `/` with a ProseMirror instance bound to a Yjs document via y-prosemirror, hardcoded room name `"vertical-slice"`, connects to local server from V-003. (test: playwright loads page, types "hello", reads it back from the DOM.)
- [x] **V-006** TUI: bootstrap Python project in `tui/` with `uv init`, add `textual`, `y-py`, `websockets`, `pytest`, `ruff`, `ty`, `prek`. (test: `uv run pytest` runs and reports 0 tests; `uv run ruff check` and `uv run ty check` pass on empty project.)
- [x] **V-007** TUI: minimal textual app that connects to the local server, joins the same `"vertical-slice"` room, and shows the Yjs document's text content in a single read-only widget. (test: pytest integration test using `textual.pilot` confirms the widget renders text after a remote Yjs update is applied.)
- [x] **V-008** TUI: enable local editing — keystrokes in the widget produce Yjs ops that propagate to the server. (test: pytest drives a TextArea via pilot, asserts the resulting Yjs document state matches expectation; second integration test asserts the change is visible via a separate y-py client.)
- [x] **V-009** End-to-end smoke (manual + scripted): run server + web + TUI, type in web, observe text appearing in TUI, type in TUI, observe text appearing in web. Record a short asciinema or screenshot in `docs/vertical-slice.md`. *(scripted parts covered by automated tests; asciinema is operator to record.)*
- [x] **V-010** Server: replace in-memory storage with Hocuspocus's SQLite extension; persist Yjs state across restarts. (test: integration test writes data, restarts the server, reads it back.)
- [x] **V-011** Confirm V-009 still works with persistence enabled. Update `docs/vertical-slice.md` with the restart-survives-data caveat.

**Phase 0 exit criteria:** all V- tasks checked. The architecture is proven; parallel workstreams can begin.

---

## Workstream R — Repo, tooling, CI

Lightweight infrastructure that the other workstreams depend on. Mostly fast.

- [x] **R-001** Root `.editorconfig`, `.gitignore`, `LICENSE` (MIT or similar). (test: lint config check or just visual review.)
- [x] **R-002** `tui/`: set up `prek` pre-commit hooks running `ruff` and `ty`. (test: `prek run --all-files` passes on a clean tree; failing test added to confirm a deliberately broken file fails the hook.)
- [x] **R-003** `server/`: set up husky or lefthook for pre-commit running `eslint` + `prettier --check` + `vitest run`. (test: hook fires on commit attempt; broken file blocks commit.)
- [x] **R-004** `web/`: same pattern as R-003 (eslint, prettier, vitest, optional `svelte-check`/`tsc --noEmit`).
- [x] **R-005** GitHub Actions workflow `.github/workflows/ci.yml`: jobs for server (Node), web (Node), tui (Python via uv) running tests + lint + typecheck on PR. (test: PR opened against `main` triggers all three jobs.)
- [x] **R-006** GitHub Actions: cache `node_modules`, `uv` venv, and Playwright browsers for fast CI. (test: second run completes faster than first; cache hit logged.)
- [x] **R-007** Root `Makefile` (or `justfile`) with targets: `dev` (run all three concurrently), `test` (run all suites), `lint`, `typecheck`. (test: `make test` exits 0 on a green tree.)

---

## Workstream D — Data layer and migrations

All migrations are SQLite. Each migration ships with a test that exercises the new schema.

- [x] **D-001** Choose and wire a SQLite migration tool (recommend `umzug` + `better-sqlite3`). Add `migrate up` / `migrate down` scripts. (test: empty DB after `up` then `down` is the same as never running; `up` is idempotent.)
- [x] **D-002** Migration 001 — `users`: id (uuid), email (unique), display_name, color, created_at. (test: insert + select round-trip; unique-email constraint rejects duplicates.)
- [x] **D-003** Migration 002 — `notes`: id (uuid), title, created_by FK users, created_at, updated_at, trashed_at (nullable), markdown_export (text). (Yjs blob lives in Hocuspocus's `documents` table, not here — see `server/src/db/README.md`.) (test: round-trip; trashed_at filter works.)
- [x] **D-004** Migration 003 — `note_titles_history`: note_id FK notes, title, valid_from, valid_to (nullable). Index on (title) and (note_id). (test: insert two titles for one note; resolve old title returns the right note.)
- [x] **D-005** Migration 004 — `tags`: note_id FK notes, tag (text). Unique (note_id, tag). Index on tag. (test: insert + dedupe; list-by-tag returns notes.)
- [x] **D-006** Migration 005 — `backlinks`: source_note_id, target_note_id, link_text. Index on both. (test: insert + query inbound returns correct sources.)
- [x] **D-007** Migration 006 — `comments`: id, note_id, author_id, parent_comment_id (nullable), anchor (text), original_quote (text), body, created_at, resolved_at (nullable). (test: insert thread; resolve sets resolved_at.)
- [x] **D-008** Migration 007 — `snapshots`: id, note_id, yjs_state (blob), created_at, label (nullable). Index on (note_id, created_at). (test: insert + list-by-note-newest-first.)
- [x] **D-009** Migration 008 — `mentions`: id, note_id, mentioned_user_id, mentioning_user_id, source (text), created_at, read_at (nullable), email_sent_at (nullable). (test: unread filter returns only unread.)
- [x] **D-010** Migration 009 — FTS5 virtual table `notes_fts` over `notes.markdown_export`, with triggers maintaining it on insert/update/delete of `notes`. (test: insert a note, FTS query returns it; rename, FTS still matches.)
- [x] **D-011** Repository layer: typed read/write functions per table, all with tests. (test: per-table unit tests cover the public API.)
- [x] **D-012** Database fixture utilities for tests: in-memory SQLite + migration up, clean teardown. (test: two consecutive tests get isolated DBs.)

---

## Workstream A — Auth

Google OAuth allowlist + session cookies (web) + device-code flow (TUI). All routes after this workstream require an authenticated session.

- [x] **A-001** Allowlist config: `BARTLEBY_ALLOWED_EMAILS` env var (comma-separated) loaded at startup. (test: unknown email rejected; allowed email accepted; missing env var fails startup loudly.)
- [x] **A-002** Google OAuth authorization-code endpoints: `GET /auth/google/start` redirects to Google; `GET /auth/google/callback` exchanges code, fetches userinfo, checks allowlist, upserts `users` row, sets session cookie. (test: full flow with mocked Google endpoints; non-allowlisted email returns 403 and no session.)
- [x] **A-003** Session middleware: signed cookie containing user id; rejects unauthenticated requests on protected routes. (test: protected route returns 401 without cookie, 200 with valid cookie, 401 with tampered cookie.)
- [x] **A-004** `GET /auth/me` returns the current user (id, email, display_name, color). (test: returns user data when authed, 401 otherwise.)
- [x] **A-005** `POST /auth/logout` clears the session cookie. (test: subsequent request is unauthed.)
- [x] **A-006** Device-code start: `POST /auth/device/start` returns `{ device_code, user_code, verification_uri, interval, expires_in }`; stores pending row. (test: returns shape; row exists.)
- [x] **A-007** Device-code verify page: `GET /device` HTML page; `POST /device/approve` looks up user_code, attaches the authenticated user, marks approved. (test: unauthenticated user is redirected to OAuth then back to /device; approving marks row.)
- [x] **A-008** Device-code poll: `POST /auth/device/poll` with device_code returns 428 while pending, 200 with refresh+access tokens once approved, 410 if expired. (test: state machine covered.)
- [x] **A-009** Token refresh: `POST /auth/token/refresh`. (test: refresh issues new access token; revoked refresh token returns 401.)
- [x] **A-010** Hocuspocus auth hook: validates Bearer token on `onConnect`; rejects unauthenticated connections; attaches user id to the connection context. (test: WS handshake without token closes; with valid token succeeds.)

---

## Workstream S — Server REST API

REST endpoints for everything that isn't live Yjs traffic. All require an authenticated session via A-003 or token via A-010. Depends on D-* migrations.

- [x] **S-001** `POST /notes` — create note; returns `{ id, title }`. Initial Yjs state is an empty ProseMirror doc. Writes `notes` and `note_titles_history` rows. (test: returns 201 with id; row exists; FTS finds it after first save.) *(Yjs state materializes on first WS connect via Hocuspocus; this endpoint only writes the metadata row + history entry.)*
- [x] **S-002** `GET /notes` — list non-trashed notes with `id`, `title`, `tags`, `updated_at`. Supports `?tag=foo` and `?q=text`. (test: list shape; tag filter; FTS query.)
- [x] **S-003** `GET /notes/trash` — list trashed notes. (test: only trashed notes returned.)
- [x] **S-004** `PATCH /notes/:id` — update title and/or tags. Title change appends to `note_titles_history`. (test: title rename history grows; tags replaced atomically.)
- [x] **S-005** `DELETE /notes/:id` — soft-delete (sets `trashed_at`). (test: note disappears from `GET /notes`, appears in `GET /notes/trash`.)
- [x] **S-006** `POST /notes/:id/restore` — clears `trashed_at`. (test: round-trip with S-005.)
- [x] **S-007** `GET /notes/:id/backlinks` — returns inbound links (source note id, title, link_text). (test: link from A to B shows A in B's backlinks.) *(also omits backlinks whose source note has been trashed — phantom-source guard.)*
- [x] **S-008** `GET /notes/resolve?title=...` — title → uuid resolver using `note_titles_history`. Returns 200 with id, 404 if unknown, 300/ambiguous response if multiple current matches. (test: all three cases.) *(historical titles resolve too — `[[OldName]]` after a rename still finds the note.)*
- [x] **S-009** Yjs change hook: on `onStoreDocument` (Hocuspocus), serialize doc to markdown, update `notes.markdown_export`, re-extract tags (from frontmatter or inline `#tag`), re-extract `[[backlinks]]`, update FTS. Debounce per note id (1–2s). (test: write to Yjs doc → tables updated within debounce window; concurrent writes coalesce.) *(debounce is Hocuspocus's built-in for SQLite WAL flush — our hook runs after that, no extra debouncing needed. FTS5 triggers update notes_fts automatically off the markdown_export UPDATE.)*
- [x] **S-010** Trash purge job: every hour, hard-delete notes with `trashed_at` older than 30 days, cascading to comments/snapshots/backlinks/mentions/tags. (test: time-traveled note older than 30 days is purged; younger note is not.) *(cascade is automatic via D's FK `ON DELETE CASCADE` on every dependent table; this PR only owns the scheduler + the cutoff math. Timer is unref()'d so it doesn't block shutdown.)*
- [x] **S-011** `GET /search?q=...` — FTS search over `notes_fts`, returns notes with snippet. (test: query matches body and title.)
- [x] **S-012** Error model: consistent JSON error responses (`{ error: { code, message } }`). (test: 4xx/5xx all match shape.)
- [x] **S-013** Request logging via pino: structured JSON, includes user id, route, status, duration. (test: log assertion via test transport.)

---

## Workstream W — Web client features

Each task ships with a Playwright test asserting the behavior end-to-end against a test server. Depends on V-* and A-005 (sign-in flow).

- [x] **W-001** Route skeleton: `/`, `/n/:id`, `/trash`, `/inbox`. Unauthed users hit `/login` and get redirected to Google. (test: routing + auth gate.) *(PR 1 ships `/`, `/n/:id`, `/login` + the auth gate. `/trash` and `/inbox` stubs land with W-022 and W-023 respectively.)*
- [x] **W-002** Sign-in page (`/login`) with a single "Sign in with Google" button. (test: click redirects to `/auth/google/start`.)
- [x] **W-003** App shell: left sidebar + main pane + collapsible right pane. Keyboard shortcut `Cmd-K` focuses search. (test: layout exists; shortcut focuses input.) *(layout shipped; Cmd-K search shortcut lands with W-020 search overlay.)*
- [x] **W-004** Sidebar: notes list, search input, tag filter chips, "New note" button. List updates live when notes are created/renamed/deleted on the server. (test: create-from-API appears in list within 1s.) *(W PR 2 ships the list + "new note" button + 1s polling for live updates. Search input + tag filter chips land in W-020 / W-021.)*
- [x] **W-005** New note flow: click button → `POST /notes` → navigate to `/n/:id` with focus in the title field. (test: click leads to editable new note.) *(form-submit to `/api/notes/new` → server proxies to bartleby POST /notes → 303 to /n/[new-id]. Default title is "Untitled".)*
- [x] **W-006** Title-in-place editor: editable `<h1>` at top of note; commits to `PATCH /notes/:id` on blur or Enter. (test: rename persists.) *(Enter triggers form-submit → server PATCH → 303 reload. Blur-to-save is deferred — Svelte 5 event delegation didn't cooperate with Playwright's programmatic blur events in this combo; Enter satisfies the core spec.)*
- [x] **W-007** Tag chip editor below the title: add tag (type + Enter), remove tag (× on chip). Commits via `PATCH /notes/:id`. (test: add/remove round-trips.) *(form-submit pattern: each chip × is its own form posting "tags minus this one"; the add form posts current `tags` + a `newtag` field, server combines + dedupes. The endpoint's newline delimiter for `tags` keeps tags-with-commas safe.)*
- [x] **W-008** ProseMirror editor with y-prosemirror, joins room `note:<id>`. Toolbar: bold, italic, strike, link, H1/H2/H3, bullet/ordered list, blockquote, code block. (test: each toolbar action produces the right ProseMirror node/mark.) *(Editor was already in place from V-005; this PR adds the 11-button toolbar, augments the schema with `prosemirror-schema-list` + a custom `strike` mark, and updates the server's S-009 markdown serializer to match (lists are handled by `defaultMarkdownSerializer`; strike gets a `~~text~~` mark entry). Link uses `window.prompt` for the URL — a real popover ships with W-009's Cmd-K. The shared schema currently lives at `web/src/lib/editor/schema.ts` + `server/src/derived/schema.ts` with a "MIRRORS" cross-reference; promote to a shared package when I-001/I-002 land.)*
- [x] **W-009** Keyboard shortcuts: `Cmd-B`, `Cmd-I`, `Cmd-Shift-X`, `Cmd-K` (link), markdown-style `#` / `##` / `- ` / `1. ` / `> ` autocomplete on empty line. (test: each shortcut.) *(Mod-K opens a LinkPopover that captures the ProseMirror selection at open time and replays it on submit — replaces W-008's `window.prompt` placeholder. Toolbar Link now routes through the same popover. Mod-K on an empty selection is a no-op. Input rules anchored at block start, so triggers mid-line stay literal.)*
- [x] **W-010** Task list rendering and toggle (click checkbox or Space when caret inside). (test: toggling persists.) *(Schema adds `task_list` / `task_item` (with `checked` attr) — mirrored web↔server. A NodeView renders each task_item as a native `<input type=checkbox>` + content area; clicking toggles via `setNodeMarkup`. Markdown autocomplete: `- [ ] ` / `- [x] ` promotes a single-item bullet_list to a task_list with the matching checked state via a custom InputRule that fires after the existing bullet rule. Space at position 0 of an empty task_item toggles; Space elsewhere stays a literal space. Toggling persists through Hocuspocus/Yjs round-trip + a page reload. Server markdown serializer renders task lists as GFM `- [ ] foo` for FTS/backlink extraction. **Includes a patch-package patch to y-prosemirror**: when an input rule wraps content (any of `- `, `> `, `1. `, `[ ] `), the ySyncPlugin's observer can fire with the new wrapper's YXmlElement visible but its children not yet integrated; the upstream code throws "Invalid content" and DELETES the wrapper from the YDoc, destroying real user content. The patch turns that delete into a no-op so the next observer fire retries successfully — this also incidentally stabilizes the pre-existing `bullet_list` / `ordered_list` / `blockquote` autocomplete that was passing only via Playwright auto-retry catching a ~100ms window. See `patches/y-prosemirror+1.3.7.patch`.)*
- [x] **W-011** Code block: language picker, syntax highlighting via Shiki. (test: rendered code block has correct token classes.) *(Schema override: `code_block` gains a `language` attr (default `'text'`) — mirrored web↔server. NodeView renders `<pre data-language><code>…</code></pre>` plus a `Lang: <id> ▾` button in the top-right corner; clicking it opens `CodeLangPopover` (mirrors `LinkPopover`) with a curated list of 13 languages (text, bash, css, go, html, js, json, md, py, rs, sql, ts, yaml) — kept narrow so the lazy-loaded Shiki bundle stays small. Apply dispatches `setNodeMarkup` against the captured block pos; Escape cancels. A ProseMirror plugin tokenizes each non-`'text'` code block via Shiki's `codeToTokens` (dynamic-imported on first use) and emits inline `Decoration`s with `class="shiki-tok-<type>"` + an inline color style. TextMate scopes are coarse-classified to a stable taxonomy (`keyword`, `string`, `number`, `comment`, `function`, …) so the spec test can assert on `.shiki-tok-keyword` without coupling to Shiki grammar internals. Tokenization runs on a 60ms debounce and is generation-counted so stale runs racing newer doc edits get dropped. Server markdown serializer emits fenced ` ```ts foo ``` ` (drops the tag when language is `'text'`).)*
- [x] **W-012** `[[backlink]]` syntax: typing `[[` opens a notes-picker autocomplete (fuzzy over titles); selecting inserts a stable backlink node. Rendered as a clickable link. (test: autocomplete populates; click navigates.) *(Schema adds an inline atom `backlink` node with `{ targetId, title }` attrs — mirrored web↔server. A ProseMirror plugin (`backlink-trigger-plugin.ts`) watches the doc for an active `[[<query>` session — the query is the text typed AFTER `[[` (Obsidian/Notion convention) and the plugin signals open/close + live query via an onChange callback so Editor.svelte can mount `BacklinkPickerPopover` reactively. The popover filters `NotesStore.notes` locally (case-insensitive substring on title, capped at 8, sorted, current note excluded) — no new server endpoint, no extra round-trip. The editor stays focused while the popover is open so typing keeps flowing into the doc; Escape is intercepted via the plugin's `handleKeyDown` (the popover itself has no focus) and triggers an `onEscape` callback. The plugin also stashes the suppressed `triggerStart` so the popover stays closed until the user types a fresh `[[`. Per the W-012 cancel-mode decision, Escape closes the popover but leaves the literal `[[query` in the doc — the S-009 onStoreDocument hook's regex extractor still resolves it server-side once the user types `]]`. Apply replaces `[[query` with a backlink atom carrying `{ targetId, title }` via a `setNodeMarkup`-style `replaceWith` transaction. A NodeView renders the atom as `<a href="/n/{id}" data-backlink>{title}</a>` and intercepts plain click — calling SvelteKit's `goto` so the spec-test "click navigates" requirement is met without a full-page reload. Server markdown serializer emits `[[{title}]]` for backlink nodes so the existing S-009 backlink-extraction regex in `derived/backlinks.ts` continues to populate the `backlinks` table unchanged; we deliberately don't export `targetId` to keep markdown exports portable across id changes.)*
- [x] **W-013** @mention syntax: typing `@` opens a friends-picker over the allowlist; selecting inserts a mention node. (test: picker shows users; selecting inserts.) *(New `GET /users` endpoint returns the union of the email allowlist and the in-memory users table — allowlist-only entries surface as `signed_in: false` with null user info; signed-in users carry `user_id` + `displayName` + `color`. The allowlist stays authoritative — signed-in users whose email is later removed from the allowlist drop off the list. Web schema adds an inline atom `mention` node with `{ email, displayName }` attrs (mirrored web↔server); the chip renders as `<span data-mention>@displayName</span>`, falling back to `@email` for allowlist-only entries. A `mention-trigger-plugin` (siblings the W-012 backlink plugin) detects active `@<query>` typing — fires only at start-of-block or after whitespace, never mid-word — and signals Editor.svelte to mount `MentionPickerPopover`; clicking a candidate dispatches `replaceWith` against the captured trigger range. Escape closes the popover and leaves the literal `@query` text in place (no derived-state extractor cares about loose `@`s in v1; M-001 will key off mention nodes, not raw strings). Server markdown serializer emits `@<email>` so a future M-001 mention-extraction pass has a stable identifier that uniquely resolves to a `user_id` without depending on collidable display names. `UsersStore` polls `/users` on a 30s cadence — the allowlist is operator-managed and changes on a redeploy cadence so sub-second freshness isn't needed.)*
- [x] **W-014** Presence cursors: render remote users' carets and selections in their assigned color with a name label above. (test: second browser session shows up as a colored cursor in the first.) *(Shipped alongside [[C-001]] — same mechanism. Editor.svelte accepts a `user` prop sourced from `+layout.server.ts` (JWT-decoded in `hooks.server.ts`, no `/auth/me` round-trip) and publishes `{ name: display_name, color }` via the HocuspocusProvider's `awareness.setLocalStateField('user', …)`. y-prosemirror's `yCursorPlugin(provider.awareness)` lives in the plugin list when `provider.awareness` is non-null (Phase 0's hardcoded fixture still mounts the editor without it). The default cursorBuilder + selectionBuilder are kept; CSS styles `.ProseMirror-yjs-cursor` as a thin colored vertical bar with a name pill floating above the caret, and `.ProseMirror-yjs-selection` rides on the inline `background-color: <hex>70` the default builder emits. No server changes — Hocuspocus relays Yjs awareness automatically over the existing WebSocket; the W-014 spec test (and C-001's two-client awareness test) is satisfied by `web/tests/editor-presence.test.ts` opening two browser contexts as alice + bob and asserting each sees the other's caret + name label.)*
- [x] **W-015** Right pane tabs: Comments / Backlinks / History. Persist open tab per note in localStorage. (test: tab choice survives reload.) *(Ships the tab SHELL only — actual content lands in W-017 (comments), W-016 (backlinks), W-019 (history). NoteRightPane.svelte owns three tabs with placeholder bodies pointing at the future PRs; AppShell renders it on `/n/[id]` (keyed on the noteId so navigating between notes remounts cleanly) and a generic "Open a note…" hint elsewhere. Active tab persists per-note in `localStorage` under `bartleby:rightpane:tab:<noteId>`; per-note keying matters because different notes have different ergonomics (a research note lives in Backlinks, a doc with active reviewers lives in Comments) and a global "last selected" would constantly stomp those preferences. Default tab on first visit is Comments. The localStorage read happens in onMount only — the earlier $effect-driven implementation raced the click handler and re-stomped the user's choice; the {#key noteId} remount + onMount approach is the simplest thing that survives Svelte 5's reactive graph quirks. The `active` $state variable was renamed to `activeTab` to avoid a subtle collision with the `class:active={active === tab.id}` directive's class-name keyword. Playwright tests wait on `editor-toolbar` as a hydration proxy before interacting with the tab buttons — without that, clicks race SvelteKit hydration and fall on the floor.)*
- [x] **W-016** Backlinks pane: shows inbound links from `GET /notes/:id/backlinks`. (test: link from A to B shows A in B's pane.) *(Drops `BacklinksPane.svelte` into the [[W-015]] Backlinks tab. Fetches once via `listBacklinks(noteId)` on mount; no polling — inbound links don't change at high frequency and the NoteRightPane's `{#key noteId}` remount handles note navigation. Renders one `<a href="/n/{source.id}">{source.title}</a>` per row, with the original link_text shown in muted `[[…]]` form when it differs from the source title (which can happen if the user typed `[[Title|alias]]` style — though the W-012 picker doesn't produce aliases yet). Empty state ("No notes link to this one yet.") and error state are explicit so the user can tell loading from "nothing here". The W-016 spec test exercises the full real flow — picker insert in A → wait for Hocuspocus debounce + S-009 hook → pane render in B — with a 15s timeout to absorb the save/extract latency.)*
- [x] **W-017** Comments pane: thread list, expanded thread shows replies, reply composer, resolve button. Comment markers in the body are numbered and clickable. (test: full CRUD.) *(Shipped alongside [[C-007]] — same PR; client side consumes the new server CRUD. CommentsPane.svelte lives in the W-015 Comments tab. Server returns a flat list of CommentDto rows and the client groups them client-side into `{ top, replies }` threads (parent_comment_id is the only structural signal we need; recursive components would be overkill for v1's one-level reply model). Each thread card surfaces: a "New comment" composer at the top of the pane (with an empty anchor — W-018 will populate it from the live selection), a "Reply" affordance per thread that toggles an inline composer keyed by thread id, a Resolve↔Reopen toggle, and a Delete affordance that prunes the thread + its replies locally (the server's FK ON DELETE CASCADE handles the same on the row side). An "Include resolved" checkbox flips the underlying `?include=resolved` query so the pane can switch between active conversation and full archive without leaving. Author labels resolve via a one-shot `listUsers()` lookup on mount (the W-013 endpoint), falling back to the raw user_id when no match is found. The "Comment markers in the body are numbered and clickable" portion of the spec — the in-body decorations driven by anchor — is part of [[W-018]] alongside the selection-driven composer; the anchor column accepts empty strings server-side so W-018 can backfill without a schema change.)*
- [x] **W-018** Comment composer: select text in body → "Comment" action in floating toolbar → composes inline; submits to comments endpoints. (test: selection produces anchor; comment renders in pane.) *(Bundled with the in-body numbered markers that were deferred from [[W-017]] — same anchor infrastructure powers both. Anchor format is `{ from, to }` of two Yjs RelativePositions, JSON-serialized into the existing `comments.anchor` text column (C-007 already accepted opaque bytes there). The (de)serializers in `comment-anchor.ts` round-trip through y-prosemirror's `absolutePositionToRelativePosition` / `relativePositionToAbsolutePosition`; orphan detection (`to` resolves to null because the anchored text was deleted) just returns null from `resolveAnchor` and the marker skips that comment — [[C-008]] will fold this into a first-class `is_orphaned` flag. A new ProseMirror plugin (`comment-selection-plugin.ts`) reports non-empty selections in normal textblocks (skips code blocks where the user is likely writing code) to Editor.svelte; the editor mounts a floating "Comment" button at the selection's bottom edge computed via `view.coordsAtPos`. Clicking swaps toolbar → `CommentComposerPopover.svelte` with the selected text shown as a muted quote, focus jumps to the body input, Cmd/Ctrl+Enter or click submits. The POST builds the anchor from the captured (from,to) and the selected text becomes `original_quote`. A second plugin (`comment-markers-plugin.ts`) reads from a shared `CommentsStore` (singleton keyed by noteId — module-level because the layout/page tree can't carry context between the editor and the right pane) and emits one `<button data-comment-marker>` widget Decoration per open, non-orphaned top-level comment, numbered 1-based in created_at order so the numbering stays stable across edits. Clicking a marker writes to `bartleby:rightpane:tab:<noteId>` (W-015 honors it on reload), sets `aria-selected=true` on the Comments tab in-memory, and dispatches `bartleby:focus-comment` — CommentsPane listens, scrolls the matching thread into view, and pings it with a 2s amber border. Editor.svelte uses `$effect.root` to subscribe to the store's reactive comments list and dispatches a no-op `refreshCommentMarkers` transaction whenever the list changes so the marker decorations rebuild without a doc edit. CommentsPane was refactored from local state to consume the same store (insertLocal/updateLocal/removeLocal helpers keep the pane and the editor in lock-step after every mutation). Playwright tests use keyboard selection (Home/End + Shift+Arrows) instead of `.dblclick()` — Playwright's synthetic dblclick events don't reliably trigger ProseMirror's mouse-handler-driven word selection. The pane's `onDestroy` guards `window.removeEventListener` behind a `typeof window` check because SvelteKit's prerender pass fires destroy hooks server-side too.)*
- [x] **W-019** Snapshots pane: list of snapshots (named + auto), preview pane, "Restore" action. (test: restore replaces document content.) *(Shipped alongside [[C-002]]/[[C-003]]/[[C-004]]/[[C-005]]/[[C-006]] — same PR; client side consumes the new snapshot routes. SnapshotsPane.svelte lives in the W-015 History tab. Lists snapshots newest-first via `listSnapshots(noteId)`; selecting a row lazy-fetches the detail endpoint which carries both `yjs_state_base64` and a pre-rendered `markdown_preview` so the client can render without pulling in Yjs. "Save snapshot" composer in the header POSTs to C-003 with a label and prepends the result to the local list. "Restore this snapshot" calls C-006 (after a confirm dialog) — the server records a pre-restore auto-snapshot of the current state, then writes the selected snapshot's bytes back into the live Yjs doc; the live editor picks up the new state on the next WebSocket round-trip. Playwright tests reload between restore and assertion so the HocuspocusProvider re-pulls the canonical state. Empty/loading/error states are explicit. No client-side polling — auto-snapshots from C-002 only appear after a reload, which matches the "history is for looking back, not real-time" mental model.)*
- [x] **W-020** Search results page or overlay: keyword search calls `GET /search`, shows snippets, clicking opens the note. (test: query → result → navigate.) *(Modal overlay (not a sidebar pane) so search-from-anywhere works without losing visual context of the underlying editor. Triggered by Cmd-K from anywhere outside the editor; AppShell hosts the SearchOverlay component which owns its own window-level keydown listener. The editor's [[W-009]] Mod-K (link popover) still wins inside the editor — we disambiguate by checking `e.target.closest('.ProseMirror')` rather than `e.defaultPrevented` because ProseMirror doesn't auto-preventDefault on consumed keys (it just returns `true` from its handler, leaving the native event alive — gotcha worth remembering for any future window-vs-PM keymap arbitration). Input is debounced 150ms; an AbortController cancels stale requests. Snippets come back from FTS5's `snippet(..., '<mark>', '</mark>')` and we parse those into `{ text, highlighted }` segments before rendering — `{@html}` would be XSS-vulnerable because note bodies are user-controlled. Up/Down/Enter for keyboard nav; Esc / click-outside / row click dismisses. Playwright tests use `input.fill()` instead of `keyboard.type()` for determinism under parallel load (focus can race the overlay's auto-focus), and the W-020 spec test polls `/search` directly to absorb the S-009 Hocuspocus-debounce + FTS5-trigger lag before opening the overlay.)*
- [x] **W-021** Tag filter: clicking a tag chip in the sidebar filters the list. Clicking again clears. (test: click cycles.) *(Sidebar renders tag chips above the notes list. The chip set is the union of every visible note's tags (sorted lowercase alphabetical for stability — a frequency-based sort would re-order chips on every poll and make them frustrating to click). Filtering is client-side off the polled NotesStore — the server's `?tag=` filter would work too, but client-side keeps the chip set stable: a server-filtered fetch would shrink the visible chip list to only the filtered tag's notes' tags, and the user couldn't see other tags to switch to. The single-tag filter is v1 simplicity; multi-tag AND/OR can ship later if telemetry wants it. Click toggles the filter; clicking a DIFFERENT chip swaps the filter (not stacks). An explanatory empty state — "No notes tagged #X." — surfaces when the filter resolves to zero notes, which usually means the user just stripped the last note carrying that tag.)*
- [x] **W-022** Trash view (`/trash`): list trashed notes; "Restore" and "Delete forever" actions per row. (test: restore round-trip with W-024.) *(Shipped alongside [[W-024]] — same PR; the spec test exercises both. New page at `/trash` SSR-loaded via `listTrashed()` (S-003 endpoint). Each row gets a Restore button (S-006) and a Delete-forever button. The hard-delete server endpoint is a new query param `DELETE /notes/:id?forever=true` that requires the note to already be trashed — a 400 short-circuits "?forever=true" on a live note, so a misclick can't skip the soft-delete safety net. FK ON DELETE CASCADE on dependent tables (comments / snapshots / backlinks / mentions / tags / note_titles_history) wipes the rest. Optimistic prune via a `hidden: Set<string>` $state filtered through a $derived `notes` list, then `invalidateAll()` re-pulls the canonical list. Delete-forever routes through the W-024 ConfirmDialog with a danger-toned button; Restore skips the modal since it's reversible-enough.)*
- [x] **W-023** Mentions inbox (`/inbox`): list of unread + recent mentions; clicking marks as read and navigates to the source. (test: unread badge clears on click.) *(Shipped alongside [[M-001]]/[[M-002]]/[[M-003]]/[[M-004]] — same PR. /inbox is SSR-loaded; the page splits mentions into Unread (with blue ring + bold title) and Earlier sections. Clicking a row fires `markMentionRead(id)` fire-and-forget AND navigates to the source note (Cmd-K Slack-style — don't block the navigation on the read-mark POST). Optimistic local read-overlay so the unread row stops looking unread before the next page load. Sidebar grows a footer Inbox link with a red badge showing unread count; the `MentionsStore` polls `/mentions?unread=true` every 5s and the badge updates on the next tick after a click.)*
- [x] **W-024** Delete confirmation modal (soft-delete) from the note view and from the list. (test: confirm soft-deletes; cancel does nothing.) *(Shipped alongside [[W-022]]. `ConfirmDialog.svelte` is a small generic modal taking `title` / `body` / `confirmLabel` / `confirmTone` ('primary' | 'danger') / `onConfirm` / `onCancel`. Owns its own Esc handler (svelte:window), backdrop-click-to-cancel (with an svelte-ignore for the a11y-click-without-key rule, since Esc serves the same role), and focuses the confirm button on mount for hot-key submitters. Two destructive entry points share it: the note view (`/n/[id]`) gets a 🗑 button next to the title that opens a "Move to trash?" modal and on confirm soft-deletes + navigates to /trash; the sidebar (`Sidebar.svelte`) grows a per-row 🗑 affordance (faded by default, opaque on row hover) with the same modal. The /trash page also uses it for the danger-toned "Delete forever?" prompt. Cancel + Esc both close the modal without side effects — the cancel test verifies the note stays out of trash.)*
- [x] **W-025** Drag-and-drop import: drop one or more `.md` files onto the notes list → POST each via import endpoint → appear in list. (test: drop 2 files → 2 new notes appear.) *(Shipped alongside [[I-001]]/[[I-002]]/[[I-003]] — same PR. Sidebar grows a drop zone (whole `<aside>` is the target) that highlights on dragover; a hidden `<input type="file">` + visible "Import .md…" label gives keyboard / a11y / Playwright the same code path. The handler filters to `.md`/`.markdown`, POSTs the file list to `/notes/import` via `importNotes()` in `lib/api/notes.ts`, then kicks `store.refresh()` for snappy UX (the 1s poll would catch them too). Hydration marker (`window.__sidebarHydrated`) gates the Playwright test so `setInputFiles` never fires before the `$effect`-attached change listener exists — the Svelte 5 onchange-property pattern occasionally missed events in this harness, same quirk W-005 documents.)*
- [x] **W-026** Export-all-as-zip button in sidebar footer. (test: download initiated; zip contains one file per note with frontmatter tags.) *(Shipped alongside [[I-005]]/[[I-006]] — same PR. Plain anchor `<a href="/export/all.zip" download="bartleby-notes.zip">` in the sidebar footer-nav alongside the Inbox link; the server's `content-disposition` header confirms the filename. Per-row .md content is fetched lazily from `notes.markdown_export` (kept fresh by the S-009 hook); fflate `zipSync` bundles everything into one in-memory zip. Empty-set case returns a valid zero-entry zip so the client doesn't need a special branch.)*
- [x] **W-027** Per-note "Copy as markdown" in a note-options menu. (test: clipboard receives expected markdown.) *(Shipped alongside [[I-004]]. "Copy as markdown" button in the note view's title row — single-button menu for v1; we'll grow it into a popover when a second action lands. Click fetches `/notes/:id/export.md` and dumps the markdown into `navigator.clipboard.writeText`. Two-second visual confirmation via a `data-state` attribute toggle ("✓ Copied" / "✗ Couldn't copy") so the user sees the gesture register without us pushing a toast component.)*
- [x] **W-028** Empty state for first-time users: friendly text + "Create your first note" CTA. (test: list empty → CTA visible; non-empty → CTA hidden.) *(initial empty state on `/` with placeholder copy; the "Create your first note" CTA wires up in W-005.)*

---

## Workstream T — TUI client features

`textual` + `y-py`. All tests use `textual.pilot` for UI assertions and pytest fixtures for server setup. Depends on V-* and A-006/A-007/A-008 (device-code flow).

> **Note (merge regression, fixed in PR #50):** merging the parallel T worktrees (PRs #46/#48/#49) left `app.py` referencing `NotesList`/`StatusBar`/`_status_bar`/`_notes_timer`/`_notes_view`/`_refresh_notes`/`_notes_poll_seconds` without importing/initializing them, so `BartlebyApp` `NameError`'d on mount — silently breaking the T-001/T-004/T-007/T-018/T-024 integration despite their `[x]`. PR #50 restores the dropped wiring. Also: textual-app tests that open a *live* websocket deadlock in the local headless sandbox (blocking `socket.send()` under `run_test`); they pass in CI — validate T integration work against CI, not local pytest.

- [x] **T-001** App skeleton: `Notes` pane (left), `Editor` pane (main), `StatusBar` (bottom). Empty placeholder content. (test: pilot snapshot of layout regions.)
- [x] **T-002** Device-code first-run: if no token in keychain, print device-code URL + code, poll until approved, store token. (test: integration test against a mock auth server runs the full flow.)
- [x] **T-003** y-py connection layer: connects to `wss://.../collab/note:<id>` with Bearer token, subscribes to a YDoc, exposes `apply_local_op(op)` and `on_remote_update(callback)`. (test: two clients on same room see each other's updates.)
- [x] **T-004** Renderer: walks the ProseMirror-compatible Yjs document and produces a textual `RichLog` (or custom widget) representation. Supports: paragraphs, H1–H6, bold/italic/strike (rich text styles), links (underline + color, footnoted), bullet/ordered lists, blockquote (left bar), task lists (`[ ]` / `[x]`), code blocks (bordered, syntax-highlighted via pygments). (test: snapshot per node type.) *(T-004 PR ships the renderer + `DocumentRenderer` Static widget; per-node-type tests in `tui/tests/test_renderer.py`. Code blocks render as bordered panels with a language title only — pygments-based syntax highlighting is deferred to T-025.)*
- [x] **T-005** Editing primitives: `insert_text`, `delete_range`, `toggle_mark`, `set_block_type`, `wrap_in_list`, `toggle_task` — each emits the corresponding Yjs op. (test: unit tests over a mock YDoc.) *(New `bartleby_tui.editing` module; each primitive mutates the `prosemirror` XML fragment in a single transaction (one Yjs op) — the write-side counterpart to the T-004 renderer that T-006's keybinds drive. 15 unit tests over a mock YDoc asserting structure through the same `ydoc_to_blocks` contract the renderer reads. **Inline-mark caveat:** y-py 0.6.2's `YXmlText` can't carry y-prosemirror formatting deltas (`insert` takes no attrs, no `format()`), so a marked span is encoded as a separate text-run child with the mark as a node-level attribute — renders + tests correctly in the TUI but does NOT round-trip to the web editor; text/headings/lists/task-state DO interop (structural). Lifting needs a y-py/pycrdt upgrade — tracked follow-up. Ships in PR #51, stacked on the PR #50 app.py mount fix.)*
- [ ] **T-006** Keybinds (vim-flavored, discoverable via `?`):
  - Insert mode by default; `Esc` to normal mode.
  - `Ctrl-B` toggle bold, `Ctrl-I` italic, `Ctrl-Shift-X` strike, `Ctrl-K` link prompt.
  - `# ` / `## ` / `### ` on empty line → heading.
  - `- ` / `1. ` / `> ` → list / blockquote.
  - `Space` on a task list line → toggle checkbox.
  - `[[` → backlink picker overlay.
  - `@` → mentions picker overlay.
  (test: each keybind covered by a pilot test.)
- [x] **T-007** Notes list pane: live list of notes (title, tags, updated_at). Live-updates via SSE or WebSocket on the REST side. (test: another client creating a note causes the list to update within 1s.)
- [ ] **T-008** Search (`/`): inline search input filters notes list via `GET /search`. (test: typing filters; Enter opens top result.)
- [ ] **T-009** Tag filter: `t` opens a tag picker; selected tags filter the list. (test: select tag → list reduced.)
- [ ] **T-010** Note CRUD: `n` new, `r` rename (modal), `d` delete (confirm), `R` restore from trash. (test: each action.)
- [ ] **T-011** Backlink follow: pressing Enter on a `[[link]]` token navigates to the linked note. (test: caret on link → Enter → editor switches.)
- [ ] **T-012** Inbound links pane (`g b`): toggle pane showing notes that link here. (test: pane visible; lists correct entries.)
- [ ] **T-013** Comments pane (`g c`): list of threads, comment markers numbered in body, composer modal on `c` (with current selection as anchor). (test: composing a comment shows it in pane + as a body marker.)
- [ ] **T-014** Comment reply + resolve: `Enter` on a thread expands it; `r` reply; `x` resolve. (test: each action.)
- [ ] **T-015** Snapshots/history pane (`g h`): list of snapshots, preview, `Enter` restores (with confirm). (test: restore replaces doc.)
- [ ] **T-016** Trash view (`g t`): list trashed notes; `R` restores, `D` deletes forever. (test: round-trip.)
- [ ] **T-017** Mentions inbox (`g i`): list unread + recent; Enter navigates and marks read. (test: unread count drops.)
- [x] **T-018** Status bar: connection state (`● live` / `○ offline — N pending`), presence (`● alice L42  ● you L42`), brief hint area. (test: presence updates when another client joins.) *(Shipped — `tui/src/bartleby_tui/status_bar.py` is a Static-derived widget with three sections (connection / presence / hint); `HocuspocusConnection` gained an awareness codec (`tui/src/bartleby_tui/awareness.py`), `on_status_change` / `on_awareness_change` callbacks, a `peer_awareness` snapshot, and `set_local_awareness` for publishing our own state. The app subscribes both signals and re-renders the bar; offline + no-peers is the default before connect. T-019 will populate the `— N pending` suffix (the API already accepts a `pending` int) and T-024 will replace the `FALLBACK_COLOR` shim once per-user colors land on `/auth/me`; the L42 line-number readout is deferred to a follow-on task since the T-018 acceptance test (`tests/test_status_bar.py::TestPresenceUpdatesOnPeerJoin`) only requires peer names to appear when another client joins.)*
- [ ] **T-019** Offline behavior: on disconnect, accept edits locally (Yjs queues), show `offline — N pending`; on reconnect, sync resolves automatically. (test: simulate disconnect, type, reconnect, assert remote sees edits.)
- [ ] **T-020** `?` help overlay: scrollable keybind reference grouped by mode. (test: opens, scrolls, dismisses.)
- [ ] **T-021** `:` command palette: fuzzy over commands and note titles. (test: types `:export`, runs export.)
- [ ] **T-022** Export single note (`:export`) writes `.md` to a chosen path. (test: file exists with expected content.)
- [ ] **T-023** Export all (`:export-all`) writes zip to a chosen path. (test: zip contains expected files.)
- [x] **T-024** Color: assign per-user colors at user creation server-side; TUI reads from `/auth/me` and applies to presence rendering. (test: two users have distinct colors.)
- [ ] **T-025** Pygments-based syntax highlighting for code blocks. (test: a Python code block renders with expected token styles.)

---

## Workstream C — Collaboration features (cross-client)

Server-side mechanisms behind comments, history, presence, mentions. The clients (W- and T-) consume these.

- [x] **C-001** Awareness wiring: Hocuspocus awareness propagates user id, name, color, cursor RelativePosition. (test: two clients see each other's awareness.) *(Shipped alongside [[W-014]] — same code path; see W-014's note for the wiring. The web client publishes `{ name, color }` via `provider.awareness.setLocalStateField('user', …)`; the cursor RelativePosition is managed automatically by y-prosemirror's `yCursorPlugin` as the local caret moves. Hocuspocus relays awareness over its existing WebSocket without any server-side configuration. C-001's two-clients-see-each-other test is `web/tests/editor-presence.test.ts` — same fixture as W-014's spec test, asserted bidirectionally.)*
- [x] **C-002** Snapshot scheduler: per note, every ~5 min, if doc changed since last snapshot, write an unlabeled snapshot row. (test: time-traveled scheduler writes one row after change, none after another tick with no change.) *(Shipped alongside [[W-019]] — `tickAutoSnapshots()` walks `notes.listAll()`, filters out trashed rows, compares the note's `updated_at` to the most-recent snapshot's `created_at`, and writes an unlabeled snapshot via the YjsDocAccessor when the note has been touched since last snapshot. `createAutoSnapshotScheduler({ intervalMs: 5*60*1000 })` wraps a `setInterval` (`unref()`'d so it doesn't block shutdown). Tests call the bare `tickAutoSnapshots` with a pinned clock — covering the spec scenario of "tick after edit writes one row; second tick with no edit writes none". The scheduler also enforces [[C-005]] retention in-tick.)*
- [x] **C-003** Named snapshots: `POST /notes/:id/snapshots { label }`. (test: row created with label.) *(Shipped alongside [[W-019]]. Author the live Yjs state via `YjsDocAccessor.read(noteId)` which opens a Hocuspocus DirectConnection, encodes the full state with `Y.encodeStateAsUpdate`, and stores the bytes in `snapshots.yjs_state`. Hocuspocus's `Document` class extends `Y.Doc` directly so the transact callback receives the doc shape without a `.document` indirection — easy footgun caught during diag. 400 on empty/whitespace label; 404 on trashed or missing note.)*
- [x] **C-004** Snapshot list: `GET /notes/:id/snapshots` paginated. (test: returns newest-first.) *(Shipped alongside [[W-019]]. Forwards `?limit=&offset=` to the repo's `listByNote`, which already orders by `created_at DESC`. The list endpoint returns SnapshotSummary (id/note_id/label/created_at) — `yjs_state` + `markdown_preview` are only on the single-snapshot detail endpoint so list responses stay bounded.)*
- [x] **C-005** Snapshot retention: prune auto-snapshots beyond the most recent 50 per note. Named snapshots exempt. (test: insert 60 auto + 5 named → 50 auto + 5 named remain.) *(Shipped alongside [[W-019]]. `repos.snapshots.pruneAutoSnapshots(noteId, 50)` was already in place; this PR calls it after every auto-snapshot insert — both inside the [[C-002]] scheduler tick AND inside the [[C-006]] restore path's pre-restore snapshot. Named snapshots stay safe because the prune SQL filters `WHERE label IS NULL`.)*
- [x] **C-006** Snapshot restore: `POST /notes/:id/snapshots/:snap_id/restore`. Writes a pre-restore auto-snapshot of current state, then applies snapshot's Yjs state to the live doc. (test: restore changes content; pre-restore snapshot exists.) *(Shipped alongside [[W-019]]. Two-step: encode the current live state and write it as an unlabeled snapshot (so the user can undo a restore by restoring back to this row), then call `YjsDocAccessor.replace(noteId, target.yjs_state)`. Restore can't be a plain `Y.applyUpdate` because that MERGES instead of REPLACES — `replaceYDocContent` in `yjs-access.ts` decodes the snapshot bytes into a throwaway Y.Doc, walks its prosemirror XmlFragment to a PM root node, and inside a single transaction on the live doc clears the live fragment + re-inserts via `prosemirrorToYXmlFragment`. The single-transaction wrapping means connected clients see one Yjs update, not a flicker through an empty doc.)*
- [x] **C-007** Comments CRUD: `POST /notes/:id/comments`, `GET /notes/:id/comments`, `POST /comments/:id/replies`, `PATCH /comments/:id/resolve`, `PATCH /comments/:id/reopen`, `DELETE /comments/:id`. Anchor is a serialized Yjs RelativePosition pair. (test: each verb.) *(Shipped alongside [[W-017]] — same PR; server side wires the existing CommentsRepository (already in place from D-007) behind a fresh comments app mounted in http.ts. Author taken from the session user; `created_at` injected via the deps.now clock so tests can pin timestamps. Anchor is stored as opaque text — PRD §6.4 describes it as a serialized Yjs RelativePosition pair, but the server doesn't parse it; [[C-008]]'s orphan-detection will work off the raw bytes. Replies inherit the parent's anchor so the in-body marker (when [[W-018]] ships it) still points at the same selection. `?include=resolved` switches list mode between open-only (default) and full archive. Validation: body must be non-empty and non-whitespace; missing parent comment / note returns 404; bad payload returns 400. Author info is NOT joined into the response — the client resolves user_id → display via the W-013 GET /users endpoint.)*
- [x] **C-008** Comment orphan detection: on every Yjs change for a note, recompute anchor resolvability; mark comments whose anchor no longer resolves. Surface via `is_orphaned` flag. (test: delete anchored text → comment becomes orphaned.) *(Shipped with [[C-009]] in one PR. Added a denormalized `is_orphaned INTEGER NOT NULL DEFAULT 0` column to `comments` via migration 010 — denormalizing keeps `GET /notes/:id/comments` from having to crack open the live YDoc on every read. The S-009 `onStoreDocument` hook now walks every comment on the note (resolved or not, via a new `listAllByNote` repo method) after each persistence pass and toggles the flag via `setOrphaned` only when the value actually changes, so we don't churn the table. The new `server/src/comments/anchor.ts` module parses the JSON-serialized RelativePosition pair the web client stores (see `web/src/lib/editor/comment-anchor.ts`) and uses `Y.createAbsolutePositionFromRelativePosition` to decide orphan status — null on either endpoint = orphaned, as does an empty/unparseable anchor (we can't confirm those still point at anything). `CommentRow.is_orphaned` is exposed as a boolean (not the raw SQLite int) via repository-level hydration, and the GET DTO is the row verbatim.)*
- [x] **C-009** Comment quote snapshot: at create time, store the currently-anchored text in `original_quote` for use when orphaned. (test: orphan retains original text.) *(Shipped with [[C-008]] in one PR. The POST `/notes/:id/comments` handler now resolves the incoming anchor against the live YDoc (via the existing `YjsDocAccessor` plumbed in from `http.ts`) and overwrites the client-supplied `original_quote` with whatever text currently sits between the anchor's two endpoints. Server-side resolution uses y-prosemirror's `initProseMirrorDoc` to rebuild the PM mapping from the YXmlFragment, then `relativePositionToAbsolutePosition` to convert each endpoint to a PM position, then `pmDoc.textBetween(from, to)` to slice — the same source of truth the web client used when capturing the anchor, so the snapshot can't drift from the doc. Falls back to the client-supplied value when: no YDoc accessor is wired (unit tests), the anchor is empty/unparseable, the doc has no PM content yet, or the anchor doesn't resolve at create time — we'd rather store the client's best guess than nothing. Once orphaned later by C-008 the row still carries this text so the UI can show "originally commented on: <quote>".)*

---

## Workstream M — @mentions and email

Depends on D-009 (`mentions` table), C-007 (comments), and a Resend API key.

- [x] **M-001** Mention extraction on Yjs change: when a note's serialized markdown changes, diff old vs new mention nodes; insert new `mentions` rows for net-new mentions. (test: adding `@alice` creates a row; editing without removing doesn't duplicate.) *(Shipped alongside [[W-023]] — the S-009 derived-state hook now also runs mention extraction. `derived/mentions.ts` `extractMentionEmails(markdown)` regex matches `@email` at start-of-string or whitespace boundary (mirrors the W-013 trigger plugin), lowercases + dedupes. The hook resolves each email via `users.findByEmail` (skip if no row), checks the existing mention set for this note's body-source (`source = 'note:<id>'`), and inserts only net-new (user, note) pairs. Re-extraction is idempotent — a save that doesn't change the mention set adds nothing. Self-mentions (note creator @-mentions themselves) are silently dropped. We do NOT delete rows when a mention is removed from the markdown: the inbox is a record of what was said, not a mirror of the current doc state.)*
- [x] **M-002** Mention extraction on comment write: when a comment is created or edited, scan body for mention nodes; insert rows. (test: comment with `@bob` creates row.) *(Shipped alongside [[W-023]]. `extractCommentMentions` in `comments/routes.ts` runs after every comment insert (top-level + reply) — same `extractMentionEmails` regex as M-001. Source = `'comment:<comment_id>'` so the same user mentioned in two different comments stays two inbox entries. Self-mentions dropped. Comments are immutable in v1 (no PATCH body endpoint), so we only need to extract on insert, not on edit.)*
- [x] **M-003** Inbox endpoint: `GET /mentions?unread=true|false` returns mentions for the current user. (test: filter by unread; pagination.) *(Shipped alongside [[W-023]]. `mentions/routes.ts`'s `GET /mentions` returns mentions for `c.get('user').id` only — refuses to leak another user's inbox. Each row carries `note_title` joined from the notes table so the client can render rows without an N+1. `?unread=true` filters to `read_at IS NULL`; omitted/`false` returns all. Newest-first ordering.)*
- [x] **M-004** Mark-as-read: `POST /mentions/:id/read`. (test: read_at set.) *(Shipped alongside [[W-023]]. Sets `read_at = now`. 403 when the mention belongs to a different user (hostile-id probe). 404 on unknown id. Drive-by during testing: extended the auth bridge so signing in (Google callback OR `/auth/dev/sign-in`) also calls `ensureUserExists` to materialize the session user in D's `users` table. Without that, mentions for users who had signed in but not yet created content would be silently dropped because `findByEmail` wouldn't find them.)*
- [x] **M-005** Resend integration with batching: on new mention row, schedule send; coalesce mentions to the same user within a 60s sliding window into one email. (test: 3 mentions in 30s → 1 send with 3 items; 2 mentions 90s apart → 2 sends.) *(In-process per-user `MentionBatcher` (`server/src/mentions/batcher.ts`) keyed on `mentioned_user_id` with sliding-window semantics — each enqueue resets a `setTimeout(windowMs)`. On fire, the queue drains into `onSend(batch)`, the pipeline (`server/src/mentions/pipeline.ts`) renders one email and marks every row's `email_sent_at`. `flushAll()` runs on SIGTERM/SIGINT (wired in `index.ts`) so pending batches don't vanish on restart. The hook in `derived/hook.ts` and the comments route both call `enqueueByMentionId(id)` fire-and-forget — never awaited from the request/save path, so the email pipeline can't block mention-row creation. `email_sent_at` is the idempotency marker: the pipeline checks `findById(...).email_sent_at` before enqueueing so a hook retry of the same row is a no-op. Sliding window picked over tumbling so a chatty mentioner doesn't repeatedly nudge the recipient — meets the spec's "3 in 30s → 1 send" example.)*
- [x] **M-006** Email template: plain HTML + text, includes mention context (snippet) and a deep link to the note. (test: rendered HTML contains expected fields; link is signed/auth-gated.) *(Pure-function `renderMentionBatch` in `server/src/mentions/email-template.ts` returns `{ subject, html, text }`. Single-mention subject is "<Mentioner> mentioned you in Bartleby"; multi-mention is "<N> new mentions in Bartleby". HTML is inline-styled (most clients strip `<style>` blocks); every dynamic field is HTML-escaped so a hostile note title can't inject scripts. Snippet extraction (`extractSnippetForRecipient` in `pipeline.ts`) grabs ±120 chars around the first `@<recipient_email>` occurrence in the note's `markdown_export`, falling back to the first non-empty line if the mention has since been edited out. Deep link is `${PUBLIC_BASE_URL}/n/{noteId}` — auth-gated by the web router's redirect-to-/login middleware, no signed magic link needed in v1.)*
- [x] **M-007** Email send failure handling: retry with backoff; log on permanent failure; do not block mention row creation. (test: simulated 5xx retried; eventual failure logged.) *(`createEmailSender` in `server/src/mentions/email-sender.ts` retries with backoff `[1s, 5s, 30s]` (4 total attempts). The Resend transport adapter classifies status codes: 4xx (except 408/429) → `PermanentEmailError`, fail-fast + log at error; 5xx + 408/429 + network throws → `TransientEmailError`, retry then log on exhaustion. Mention-row creation can never block on the email path — the batcher fires asynchronously 60s after enqueue and the hook/route call `enqueueByMentionId` synchronously without awaiting. On permanent or exhausted failure the row's `email_sent_at` stays NULL so a future scan-and-resend job (out of scope) can recover. `RESEND_API_KEY` remains `.optional()` in `config.ts` — when unset, `createNoopEmailTransport` logs at info instead of calling Resend so local dev / CI exercise the full pipeline without outbound HTTP.)*

---

## Workstream I — Import / export

Depends on a ProseMirror-markdown serializer (shared util used by S-009 too) and on the auth/REST baseline.

- [x] **I-001** Markdown→ProseMirror parser as a server-side utility, supporting all v1 markdown nodes (PRD §6.2). (test: round-trip cases for each node type.) *(Shipped in `server/src/import/parser.ts`. Built on `prosemirror-markdown`'s `MarkdownParser` configured against `derived/schema.ts` (the mirror of `web/src/lib/editor/schema.ts`). Nodes covered: paragraph, heading H1–H6, blockquote, bullet/ordered/list_item, task_list/task_item (GFM `- [ ] / - [x]`), code_block with language (fenced ```lang block; bare fence → `language: 'text'`), hard_break. Marks: em / strong / link / code / strike (`~~text~~` via markdown-it's strikethrough rule). Inline atoms post-processed from text spans: `[[Title]]` → backlink node, `@local@domain.tld` → mention node. Round-trip tests in `parser.test.ts` parse → serialize via `extractMarkdown` (the existing S-009 serializer — see I-002) and pin stability for every node type. Frontmatter: separate `parseMarkdownDocument()` extracts a YAML block, returns `{ pmDoc, frontmatter: { title, tags, raw } }`. Malformed YAML propagates (no silent swallow) so the import endpoint surfaces a 400.)*
- [x] **I-002** ProseMirror→Markdown serializer as a server-side utility, same coverage. (test: round-trip identical for canonical inputs.) *(Serializer landed via W-pr21 export work; this PR pins the dual purpose with a header comment in `server/src/derived/markdown.ts` and adds round-trip coverage for the import use case via the I-001 parser tests. Coverage matches PRD §6.2: paragraph, H1–H6, bold/italic/strike, link, bullet/ordered list, blockquote, task list, fenced code (with language), plus the project-specific backlink + mention inline atoms.)*
- [x] **I-003** Import endpoint: `POST /notes/import` (multipart), accepts one or more `.md` files; creates notes with parsed initial Yjs state and frontmatter tags applied. (test: 2-file upload → 2 notes with tags.) *(Shipped in `server/src/import/routes.ts`. Multipart via Hono's native `c.req.formData()` — no extra plugin needed (Hono uses the WHATWG `FormData`). For each file: `parseMarkdownDocument()` → `notes.insert()` + `noteTitlesHistory.append()` → seed Yjs state by building a throwaway `Y.Doc`, applying the parsed PM tree via `prosemirrorToYXmlFragment`, encoding with `Y.encodeStateAsUpdate`, and handing the bytes to the same `YjsDocAccessor.replace()` C-006 uses for snapshot restores → `tags.replaceForNote()` for frontmatter tags. Title precedence: frontmatter title → filename minus `.md` extension → `Untitled`. Returns 201 `{ notes: [{ id, title }] }`. Rejects non-`.md` files and empty uploads with 400; malformed YAML wrapped as a ValidationError so the client gets a useful message. Mounted before the dynamic `/notes/:id` routes so the static `/notes/import` path wins.)*
- [x] **I-004** Single-note export: `GET /notes/:id/export.md`. Frontmatter includes tags. (test: response is markdown with frontmatter.) *(Shipped alongside [[W-027]]. `export/routes.ts` reads from `notes.markdown_export` (kept fresh by S-009) + `tags.listForNote` and runs both through `buildExportMarkdown`. Frontmatter is YAML-ish: `title: <quoted-if-needed>` + `tags: [a, b]` inline-array. Quoting rule: any value containing whitespace, `:`, `#`, quotes, backslash, or newline gets wrapped in double quotes with internal `"`/`\` escaped — round-trips through [[I-001]]'s frontmatter parser cleanly. Trashed notes return 404. Content-Disposition carries a slugified filename so the browser's "save as" defaults are sane.)*
- [x] **I-005** Export-all-as-zip: `GET /export/all.zip`. (test: zip contains N files for N notes; filenames are slugified titles.) *(Shipped alongside [[W-026]]. Bundles every live (non-trashed) note via `fflate`'s `zipSync` into one in-memory archive. Each entry is the same `buildExportMarkdown` output [[I-004]] produces, keyed by the slug filename [[I-006]] assigns. Trashed notes are excluded. Empty-set case returns a valid zero-entry zip (fflate handles that). `application/zip` content-type + `bartleby-notes.zip` content-disposition so the browser saves rather than navigates.)*
- [x] **I-006** Title collisions in export: append short id suffix when slugified titles collide. (test: two notes with same title produce distinct filenames.) *(Shipped alongside [[W-026]]. `assignZipFilenames` walks the list in order: first occurrence of a slug keeps the plain `<slug>.md`, second-and-later get `<slug>-<first-8-of-uuid>.md`. Order comes from `listLive()` (newest-first), so a freshly-renamed clone takes the suffix and the older incumbent keeps the clean name. Slugify rules: lowercase, strip everything outside `\w\s-`, collapse runs of dashes, fall back to `untitled` when the result is empty.)*

---

## Workstream X — Mobile read-only

Depends on web app baseline (W-001 to W-003).

- [x] **X-001** Responsive breakpoint at ~768px; below that, switch to mobile shell. (test: viewport resize toggles layout.)
- [x] **X-002** Mobile shell: single-column, top bar with title, hamburger reveals notes list as a slide-over. No editor toolbar. (test: layout per Playwright mobile emulation.) *(hamburger/notes-list deferred: no notes-list exists until Workstream W ships; the shell uses a topbar only for v1.)*
- [x] **X-003** Mobile note view: rendered (read-only) ProseMirror without editing affordances. Comments visible inline (read-only). (test: tapping in body does not open the keyboard / editor.) *(comments come with C; v1 just renders body.)*
- [x] **X-004** "Open on desktop" banner with `mailto:` link prefilled with the current note URL. (test: link href contains the URL.)

---

## Workstream O — Ops, deploy, backups

Targets a single small VPS. Depends on the server being launchable as a single Node process.

- [x] **O-001** `server/Dockerfile`: multi-stage build, slim runtime, non-root user. (test: `docker build` succeeds; image runs and serves `/health`.) *(Dockerfile written; `docker build` smoke deferred — Docker not installed in this dev env. CI/deploy will exercise it.)*
- [x] **O-002** `server/healthcheck`: `GET /health` returns 200 with DB connection check. (test: returns 200 when DB ok, 503 when DB broken.)
- [x] **O-003** `ops/docker-compose.yml`: services `bartleby`, `caddy`, `litestream`. Volumes for SQLite, Caddy data, Litestream config. (test: `docker compose up` brings all three to healthy.) *(compose authored + `.env.example` committed; live `up` deferred until deploy host exists.)*
- [x] **O-004** `ops/Caddyfile`: subdomain config with auto TLS via Let's Encrypt, reverse proxy to `bartleby:port`, WebSocket upgrade for the Hocuspocus endpoint. (test: TLS handshake works against a staging hostname.) *(Caddyfile authored; ACME exchange happens on first deploy.)*
- [x] **O-005** `ops/litestream.yml`: continuous replication of `bartleby.db` to S3-compatible bucket (env-driven endpoint/key). (test: write to DB → replicated object updates within seconds.) *(config authored; live S3 round-trip deferred to deploy.)*
- [x] **O-006** Backup restore runbook in `ops/RESTORE.md`: step-by-step Litestream restore to a fresh VPS. (test: dry-run restore on a separate disk produces a DB whose content matches.) *(runbook written + includes a dry-run section; live verification awaits an actual bucket.)*
- [x] **O-007** Deploy script `ops/deploy.sh`: SSH to VPS, `git pull`, `docker compose up -d --build`. Idempotent. (test: re-running on an up-to-date repo is a no-op.) *(written + bash-syntax-checked; idempotence is structural — `git pull --ff-only` is a no-op when up-to-date and compose `up -d` skips rebuilds when there's no change.)*
- [x] **O-008** Env var schema validation at server startup using `zod` or equivalent. Required: `BARTLEBY_ALLOWED_EMAILS`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `SESSION_SECRET`, `RESEND_API_KEY`, `PUBLIC_BASE_URL`, `LITESTREAM_BUCKET`, `LITESTREAM_ACCESS_KEY`, `LITESTREAM_SECRET_KEY`. (test: missing required var fails startup with a clear message.) *(zod schema in `server/src/config.ts`. A/M-owned secrets stay `.optional()` with TODO comments to flip to required when those workstreams ship — flipping now would block local dev. Litestream creds live in `ops/.env`, not server process env, so they're not in the server schema.)*
- [x] **O-009** Pino structured logging configured for production (JSON to stdout, redactions for secrets). (test: log contains expected fields; secrets are redacted.)
- [x] **O-010** Crash-safety smoke test: kill `bartleby` mid-edit; confirm restart reconnects clients and no data is lost. (test: scripted integration test.)
- [x] **O-011** Migrations run automatically on `bartleby` container startup, idempotent. (test: starting twice does not error.) *(entrypoint hook in `server/src/migrate.ts` runs unconditionally on `main()` startup; no-op until Workstream D-001 wires umzug.)*

---

## Workstream Q — Cross-cutting quality

- [x] **Q-001** End-to-end test: two browser sessions on the same note, simultaneous typing, no data loss. (test: Playwright with two contexts.)
- [ ] **Q-002** End-to-end test: web + TUI on the same note, simultaneous typing, no data loss. (test: pytest spinning up Playwright via subprocess + a TUI client.)
- [x] **Q-003** End-to-end test: full sign-in → create note → comment → @mention → email-sent assertion (mocked Resend). (test: scripted.)
- [x] **Q-004** Load: simulate 5 concurrent users editing 5 notes for 10 min; assert no errors, snapshot growth bounded, memory stable. (test: scripted; thresholds documented.)
- [x] **Q-005** Accessibility pass on web: keyboard-only navigation works; aria-labels on icon buttons; color contrast on presence cursors readable in light + dark. (test: axe scan on key routes.)
- [x] **Q-006** Stabilize pre-existing flaky Playwright tests: `editor-toolbar` strike-through assertion, `editor-shortcuts` Mod-Shift-X path, `editor-mentions` picker-click interaction, `search-overlay` Escape dismissal. Each reproduces intermittently on `main` independent of any recent PR. (test: 20x repeat-each green for each spec; document root cause per spec in the commit message.)

---

## Launch checklist

Final gates before sharing the URL with friends.

- [ ] **L-001** `BARTLEBY_ALLOWED_EMAILS` populated with all friends' emails.
- [ ] **L-002** OAuth Google project configured: redirect URI registered for the prod subdomain; consent screen approved (test users only is fine).
- [ ] **L-003** Domain DNS pointed at the VPS; Caddy successfully provisions a Let's Encrypt cert.
- [ ] **L-004** Litestream replication active and verified via a test restore on a separate machine (O-006).
- [ ] **L-005** Operator runs through full happy path on prod (sign-in, create, edit, comment, mention, history, export).
- [ ] **L-006** README contains TUI install instructions (`uv tool install` or equivalent) and the device-code first-run walkthrough.
- [ ] **L-007** Seed any existing markdown notes via import (W-025).
- [ ] **L-008** Invite friends: send each a one-paragraph email with the URL, the TUI install line, and a note to ping the operator if anything breaks.
- [ ] **L-009** One-week soak: operator and ≥2 friends use it daily; no critical bugs (data loss, repeated disconnects, auth failures).
- [ ] **L-010** Post-soak retro: list any sharp edges, file follow-up tasks in this file with new IDs.
