# Node
node_modules/
dist/
build/
.svelte-kit/
*.tsbuildinfo

# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.ty_cache/

# Env / secrets
.env
.env.local
.env.*.local

# Databases
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
.dev-data/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Test artifacts
playwright-report/
test-results/
coverage/

# Husky internals (the actual hooks under .husky/ stay tracked)
.husky/_/

# Claude Code personal settings (machine-specific paths)
.claude/settings.local.json

# Sub-agent worktrees (live inside the project so spawned agents can reach them)
.worktrees/

# Portfolio/job-search working brief — internal, names target companies and
# interview framing. Kept local; must never land in this public repo.
RECRUITER-PRIMER.md
